{
    "models_root": "./available_models",
    "models": [
        {
            "id": 100,
            "model": "model_minimal.pt",
            "timeout": -1,
            "on_timeout": "to_cpu",
            "load": true,
            "opt": {
                "gpu":-1,
                "beam_size": 1,
                "replace_unk":true,
                "verbose":true
            }
        }
    ]
}
}
