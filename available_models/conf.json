{
    "models_root": "./available_models",
    "models": [
        {
            "id": 100,
            "model": "model_minimal.pt",
            "timeout": -1,
            "on_timeout": "unload",
            "load": false,
            "opt": {
                "gpu":-1,
                "beam_size": 1,
                "replace_unk":true,
                "verbose":true
            }
        }
    ]
}
}
